{
    "default": {
        "paths": ["src/test/features/addPost.feature"],
        "formatOptions":{
            "snippetInterface": "async-await"
        },
        "require": ["src/test/steps/*.ts"],
        "requireModule": ["ts-node/register"]
    }
}